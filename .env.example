# ── Required ──────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-...              # OpenAI API key (required for interview + scoring)

# ── Optional: Model Selection ─────────────────────────────────────────────
# OPENAI_CHAT_MODEL=gpt-5.2                       # Chat model for interviewer & LLM scorer
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small    # Embedding model for embedding scorer

# ── Optional: Scoring Thresholds ──────────────────────────────────────────
# LOW_EXTRAVERSION_THRESHOLD=2.3     # Score ≤ this → "Low"
# HIGH_EXTRAVERSION_THRESHOLD=3.6    # Score ≤ this → "Medium", above → "High"

# ── Optional: Neo4j (falls back to local JSON if unavailable) ─────────────
# NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=your-password

# ── Optional: Logging ─────────────────────────────────────────────────────
# LOG_LEVEL=INFO                     # DEBUG | INFO | WARNING | ERROR
# LOG_FORMAT=text                    # text | json (json recommended for production)

# ── Optional: Web Server ──────────────────────────────────────────────────
# PORT=8080                          # HTTP port for FastAPI
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080   # CORS origins (comma-separated)
